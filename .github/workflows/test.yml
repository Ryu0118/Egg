name: Test

on:
  push:
    branches: [main]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'E2ETestsPackage/**'
      - 'Package.swift'
      - 'Package.resolved'
      - '.swiftlint.yml'
      - '.swift-ast-lint.yml'
      - '.swiftformat'
      - 'nestfile.yaml'
      - 'scripts/nest.sh'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [main]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'E2ETestsPackage/**'
      - 'Package.swift'
      - 'Package.resolved'
      - '.swiftlint.yml'
      - '.swift-ast-lint.yml'
      - '.swiftformat'
      - 'nestfile.yaml'
      - 'scripts/nest.sh'
      - '.github/workflows/test.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  swiftlint:
    name: Lint
    runs-on: macos-26
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Bootstrap dev tools
        run: ./scripts/nest.sh bootstrap nestfile.yaml

      - name: SwiftFormat
        run: .nest/bin/swiftformat --lint --config .swiftformat .

      - name: SwiftLint
        run: .nest/bin/swiftlint lint --config .swiftlint.yml --strict --no-cache

      - name: My Swift Linter
        run: .nest/bin/my-swift-linter --config .swift-ast-lint.yml --no-cache Sources/EggKit Package.swift

  unit-tests:
    name: Unit Tests
    needs: swiftlint
    runs-on: macos-26
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run EggKitTests
        run: swift test

  e2e-tests:
    name: E2E Tests
    needs: unit-tests
    runs-on: macos-26
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run E2E Tests
        working-directory: E2ETestsPackage
        run: swift test
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
